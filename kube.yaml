apiVersion: apps/v1
kind: Pod
metadata:
  name: python
spec:
  selector:
    matchLabels:
      app: python
  replicas: 2 # tells deployment to run 2 pods matching the template
  template:
    metadata:
      labels:
        app: python
    spec:
      containers:
      - name: python
        image: python:3.8-slim-buster
        ports:
        - containerPort: 5000
